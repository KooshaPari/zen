# zen Specification

## Overview

A minimal/infrastructure project in the shelf. Contains foundational configuration files (mise.toml, cliff.toml, GitHub workflows) without application code. Serves as a baseline or template for new shelf projects.

## Contents

- `mise.toml` - Task definitions (format, lint, test, build, audit, docs:build)
- `cliff.toml` - Conventional commit configuration
- `.github/workflows/ci.yml` - CI pipeline
- `.github/workflows/release.yml` - Release automation

## Structure

Minimal project skeleton - no source code yet.

## Dependencies

None (placeholder/baseline project)